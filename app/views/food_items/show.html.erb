<nav class="navbar navbar-expand-lg navbar-light bg-light">
<a class="navbar-brand" href="/">CoderRestaurant</a>
</nav>

<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row justify-content-center">
    <div class="card" style="width: 40%">
      <%= image_tag(@food_item.img_url, class: 'card-img-top') %>
      <div class="card-body">
        <h5 class="card-title"><%= @food_item.name %></h5>
        <p class="card-text"><%= @food_item.description %></p>
        <%= link_to 'Edit', edit_food_item_path(@food_item) %> |
        <%= link_to 'Back', food_items_path %>
      </div>
    </div>
  </div> 
</div>
